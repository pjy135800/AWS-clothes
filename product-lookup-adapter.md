# Product Lookup Adapter

이 프로토타입에서 `원본 상품 정보 가져오기` 버튼은 아래 API로 실제 상품 후보를 표시합니다.

```http
GET /api/product-lookup?store=musinsa&brand=인템포무드&name=하프%20위트%20버튼%20스트라이프%20니트_블랙&option=블랙.XL%20/%201개
```

응답 형식:

```json
{
  "results": [
    {
      "store": "musinsa",
      "brand": "인템포무드",
      "name": "하프 위트 버튼 스트라이프 니트_블랙",
      "price": "42,300원",
      "image": "https://...",
      "url": "https://www.musinsa.com/products/..."
    }
  ]
}
```

## 왜 서버가 필요한가

브라우저에서 `file://index.html`만 열어 무신사 페이지를 직접 가져오면 CORS, 봇 차단, 로그인/세션, HTML 구조 변경 문제에 막힐 수 있습니다. 따라서 실제 제품 매칭은 서버에서 처리하고, 브라우저는 결과 후보만 받아 보여주는 구조가 좋습니다.

## 권장 처리 흐름

1. OCR이 주문내역 텍스트를 상품 단위로 정리합니다.
2. 서버가 `브랜드 + 상품명 + 옵션`으로 무신사 검색을 수행합니다.
3. 검색 결과에서 상품명 유사도, 브랜드 일치, 색상/품번 일치를 점수화합니다.
4. 상위 1~3개 후보의 이미지, 상품명, 가격, URL을 반환합니다.
5. 사용자가 맞는 후보를 선택하면 옷장에 등록합니다.

## 무신사 예시 매칭 기준

- 브랜드가 정확히 같으면 가산점
- 상품명 토큰이 많이 겹치면 가산점
- `_블랙`, `네이비`, `DARK-NAVY`, `UR124CWT23` 같은 색상/품번이 일치하면 큰 가산점
- 주문내역 구매가와 현재 판매가가 다를 수 있으므로 가격은 보조 신호로만 사용

## 현재 서버 구현

`server.js`는 무료 실험용으로 아래 방식으로 동작합니다.

1. `브랜드 + 상품명 + 옵션 + site:musinsa.com/products`로 DuckDuckGo HTML 검색을 호출합니다.
2. 검색 결과에서 무신사 상품 URL을 추출합니다.
3. 상품 상세 페이지의 Open Graph/JSON-LD 메타데이터에서 이미지, 상품명, 브랜드, 가격을 읽습니다.
4. 토큰 일치도를 기준으로 상위 후보 3개를 반환합니다.

이 방식은 API 키가 필요 없지만 검색 결과와 쇼핑몰 HTML 구조에 의존하기 때문에 실험용입니다.

## 다음 구현에서 붙일 기능

- 더 안정적인 `musinsa` 어댑터
- `zara` 어댑터
- `0410` 어댑터
- 검색 실패 시 사용자가 직접 상품 URL을 붙이면 상세 페이지에서 이미지/상품명을 가져오는 보정 플로우
